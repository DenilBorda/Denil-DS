#include<stdio.h>
//#include<string.h>
int main()
{
    char str[50];
    int i,length=0; 
     printf("\n enter a string  : ");
  
     gets(string);
 
     for (i=0; str[i]!='\0'; i++)
     {
        length++;
     }
     printf("the length of a string\n"); 
     printf("\n string len of %s=%d",string, length);
         
}